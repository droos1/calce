[package]
name = "calce-core"
version = "0.1.0"
edition = "2024"

[dependencies]
rust_decimal = "1"
thiserror = "2"
chrono = { version = "0.4", default-features = false, features = ["std"] }

[dev-dependencies]
rust_decimal_macros = "1"
